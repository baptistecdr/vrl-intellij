package eu.bcosp.vrlintellij.functions

val allFunctions = arrayFunctions + checksumFunctions + codecFunctions + coerceFunctions + convertFunctions +
        cryptographyFunctions + debugFunctions + enrichmentFunctions + enumerateFunctions + eventFunctions +
        ipFunctions + mapFunctions + metricsFunctions + numberFunctions + objectFunctions + parseFunctions +
        pathFunctions + randomFunctions + stringFunctions + systemFunctions + timestampFunctions + typeFunctions
